{{#each @genes as |gene|}}
<div class="row">
  <div class="title-column" {{on 'click' (fn this.showMessage gene)}}>
    {{gene}}
    {{!-- <span class="small-text">
      {{@caption}}
    </span>  --}}
  </div>
  <ToggleButton />
  <ToggleButton />
  <ToggleButton />
</div>
{{/each}}